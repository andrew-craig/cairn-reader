---
id: bug_8123
title: H12: a second 401 after a successful token refresh is silently returned to the caller
type: bug
status: closed
priority: 2
labels: [quality,frontend,mobile,web,auth]
blocked_by: []
parent: epic_fefa
remote_task_url: null
created_at: 2026-09-12T12:13:03Z
updated_at: 2026-09-15T09:17:03Z
---
Split out of task_47c1 on 2026-09-12: this is a live bug on main on BOTH platforms, independent of the apps/shared consolidation. Originally filed as finding H12 in docs/CODE_QUALITY_REVIEW.md.

## Problem
`fetchWithAuth` refreshes once on a 401 and retries the request. If the retried request *also* returns 401, the 401 `Response` is handed back to the caller as an ordinary error response instead of being treated as a dead session:

- `apps/mobile/src/services/auth.ts:529` — `return retryResponse;` with no status check.
- `apps/web/src/services/auth.ts:342` — `return await fetch(url, { ...options, headers: buildHeaders(newAccessToken ?? '') });`

Only a *thrown* refresh failure logs the user out. A refresh that succeeds but yields a token the server still rejects leaves the user sitting in a broken authenticated UI, re-issuing requests that can never succeed.

## What to do
After the retry, if the response is 401, clear tokens and throw `Session expired. Please log in again.` — the same path the catch block already takes.

## Hazard — the message string is load-bearing
`apps/mobile/src/utils/retry.ts:28` decides retryability by lowercased substring match on `error.message`. The literal `Session expired. Please log in again.` must be used verbatim; any rephrasing silently makes the error *retryable* and the client will spin on a request that can never succeed. (That prose-matching classifier is itself weak — the orphaned branch 19ae572 replaced it with an HTTP-status-based `isRetryable`, which has not landed and is not yet filed as a task. Do not depend on that change here; just keep the string intact.)

Do not swallow mobile's `NetworkError` distinction: an unreachable server during the retry must keep behaving as it does today (throw NetworkError, keep tokens) — only a genuine second 401 clears tokens.

## Done when
- Mobile and web tests each assert that a 401 -> refresh ok -> 401 sequence clears tokens and throws the session-expired message, and both fail against the current return-the-response code.
- Existing single-401-then-success and NetworkError-during-retry tests still pass.

## Notes
The orphaned branch `tier4/mobile-fetchwithauth` (commit 19ae572, ex-PR #365) contains a working fix for this in its shared module; the approach is sound but the surrounding code has drifted. See task_47c1.

## Resolution (2026-09-15)

The referenced `apps/mobile/src/services/auth.ts:529` and `apps/web/src/services/auth.ts:342`
locations no longer exist — task_47c1 (#397) had already consolidated `fetchWithAuth`
into `apps/shared/src/services/auth.ts` before this task was picked up, per bug_ad04's
resolution note. There is now exactly one copy of this logic, shared by both platforms.

Fixed in `apps/shared/src/services/auth.ts` `fetchWithAuth`: the retried request's response
is now checked for a 401, and if so a plain `Error` is thrown so it falls into the existing
catch block's non-`NetworkError` branch, which clears tokens and throws
`Session expired. Please log in again.` verbatim — no new error-handling path, just routing
the second-401 case into the path that already existed for a thrown refresh failure.

**Tests.** `apps/shared/src/services/auth.test.ts`'s characterization test (previously
"currently returns a second 401 to the caller instead of ending the session (bug_8123)") is
inverted to assert tokens are cleared and the session-expired message is thrown; it fails
against the pre-fix code. Existing single-401-then-success and NetworkError-during-retry
tests are unchanged and still pass. Since mobile and web both consume this single shared
implementation (web re-exports it unchanged, mobile subclasses it without overriding
`fetchWithAuth`), one shared-suite test covers both platforms — mobile's own
`authFetch.test.ts`/`authNetworkErrors.test.ts` (41 tests) and web's read-service tests
were run unmodified and still pass, confirming no separate platform-specific test was needed
or would avoid duplicating the shared suite's coverage.
