---
id: task_9427
title: Add golangci-lint job and create .golangci.yml config
type: task
status: in_progress
priority: 2
labels: [ci,backend]
blocked_by: []
parent: epic_d014
created_at: 2026-04-01T03:38:20Z
updated_at: 2026-04-02T03:00:46Z
---
Add golangci-lint to go-checks.yml and create a shared .golangci.yml configuration file.

Setup needed:
1. Create .golangci.yml at repo root (or per-service, TBD) with a sensible default config
2. Use golangci/golangci-lint-action in the workflow for efficient caching
3. Start with a conservative set of linters (defaults + errcheck, govet, staticcheck, unused)
4. Can expand linter set incrementally

The users service Makefile already references golangci-lint (make lint runs golangci-lint run ./...) but no config file exists and the tool isn't installed. The explore and read services just use go fmt + go vet for their lint target.

Tooling status: NEEDS SETUP - no .golangci.yml exists. golangci-lint-action handles installation in CI.
Runs standalone: YES.

Consider: one shared config at repo root vs per-service configs. Shared is simpler to maintain.
