---
id: task_2966
title: [Audit/Tier 2] Delete the dead ownership/authorization middleware package services/users/internal/middleware (196 lines)
type: task
status: open
priority: 2
labels: [quality,deletion,audit]
blocked_by: []
parent: epic_fefa
remote_task_url: null
created_at: 2026-08-17T10:14:03Z
updated_at: 2026-08-17T10:14:03Z
---
**Source:** Cairn Simplification Audit (read-only pass at HEAD `a6c56a1`, 2026-08-16) — https://claude.ai/code/artifact/286883fb-3f93-49c4-942f-4880251a409f
Read docs/QUALITY_REMEDIATION_STRATEGY.md §0 (rules of engagement) and §2.6 (definition of done) before starting. One finding, one branch, one PR. Re-verify before fixing — all file:line references below were confirmed at `a6c56a1`.

**Audit tier:** 2 (deletions — near-zero risk, compiler-verified).

## Problem
`services/users/internal/middleware` is a **196-line package with zero importers**. `grep -rn 'users/internal/middleware' --include=*.go .` returns nothing outside the package itself.

It exports four middlewares, none reachable:
- `authorization.go:15` — `RequireSameUser()`
- `authorization.go:69` — `RequireSameUserWithCustomParam(paramName string)`
- `authorization.go:127` — `RequireOwnership(ownerCheckFunc OwnerCheckFunc)`
- `authorization.go:170` — `CheckUserIDParam(paramName string)`

It has a test file (`authorization_test.go`), so the package is *tested* but never *used* — green tests over a code path nothing calls, which is precisely the 'deception' axis the strategy's §1.2 warns about: it reads as an authorization control that is in force, and it is not.

## What to do
1. Re-confirm zero importers, and **check the live routers** (`services/users/internal/handlers/router.go`) to confirm same-user/ownership enforcement is genuinely handled elsewhere — if some route is relying on nothing at all, that is a separate security finding: **stop and report it, do not just delete.**
2. If enforcement is confirmed present elsewhere, delete the package (implementation + test).

## Done when
- Either the package is deleted and `services/users` builds green, **or** a gap in live authorization enforcement is documented as a new finding.

## Note
Assume nothing about intent here — 'dead authorization middleware' has two very different readings (obsolete duplicate vs never-wired control), and step 1 is what distinguishes them. The audit verified only that it is unreferenced, not which of the two it is.
