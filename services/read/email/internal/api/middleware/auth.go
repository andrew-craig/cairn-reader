// Package middleware provides HTTP middleware for the email ingest service.
package middleware

// JWTAuth provides JWT authentication middleware for user-facing endpoints.
// Validates JWT tokens using public key from Vault.
